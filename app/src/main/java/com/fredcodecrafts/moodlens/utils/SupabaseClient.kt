package com.fredcodecrafts.moodlens.utils

// Deprecated: Logic moved to individual RequestRepositories
@Deprecated("Use specific repositories instead")
object SupabaseClient {
    // Kept to avoid breaking any other obscure references, but functionality is stripped or moved.
    // Ideally this file should be deleted once we confirm no other usages exist.
}
